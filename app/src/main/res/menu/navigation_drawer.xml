<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/main_item"
        android:title="@string/mail_header">
        <menu>
            <item
                android:id="@+id/inbox_item"
                android:icon="@drawable/ic_inbox_foreground"
                android:title="@string/inbox_text"
                android:checkable="true"
                />

            <item
                android:id="@+id/outbox_item"
                android:icon="@drawable/ic_outbox_foreground"
                android:title="@string/outbox_text"
                android:checkable="true"/>

            <item
                android:id="@+id/favourites_item"
                android:icon="@drawable/ic_favourites_foreground"
                android:title="@string/favourites_text"
                android:checkable="true"/>
        </menu>
    </item>
</menu>